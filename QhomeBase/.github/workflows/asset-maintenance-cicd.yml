name: asset-maintenance-service CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'asset-maintenance-service/**'
      - 'deployment/docker-compose/**'
  workflow_dispatch:

jobs:
  call-build:
    uses: ./.github/workflows/reusable_build.yml
    with:
      service_name: asset-maintenance-service
      docker_image: ${{ secrets.DOCKER_USERNAME }}/asset-maintenance-service